# API base URL. In dev, leave as /api (Vite proxies to the backend on :8000).
# In production (Vercel), set this to your deployed backend URL, e.g.
# VITE_API_BASE=https://portfolio-tracker-api.onrender.com
VITE_API_BASE=/api
